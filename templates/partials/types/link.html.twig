{% block type %}
  <header>
    <a href="{{ post.url }}">{{ post.title }}</a>
  </header>
  
  <p>
    {{ post.description }}
  </p>
  
  <footer>
    <small>{{ post.excerpt }}</small>
    <em>{{ post.author }}</em>
  </footer>
{% endblock %}